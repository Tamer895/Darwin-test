import { domain } from "../domain";

export const ANNOUNCEMENTS_API_ROUTES = {
    CREATE: `${domain}/announcements/announcement/`,
    POST: `${domain}/announcements/announcement/`,
    PATCH: `${domain}/announcements/announcement/`,
    PUT: `${domain}/announcements/announcement/`,
    DELETE: `${domain}/announcements/announcement/`,
    GET_ALL: `${domain}/announcements/announcement/`,
    GET_BY_ID: `${domain}/announcements/announcement/`
}